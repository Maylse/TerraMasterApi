APP_NAME=Laravel
APP_ENV=production  # Change to production
APP_KEY=base64:TMBg0PYe4n71DyCSVDnkAErEo8vkunHy506C/ne8oFk=  # Keep this as is
APP_DEBUG=false  # Set to false for production
APP_TIMEZONE=UTC
APP_URL=https://terramasterapi.onrender.com  # Update with your Render app URL

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=192.168.254.154  # Update with Render's database host
DB_PORT=3306
DB_DATABASE=laravelapitutorial
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1  # Usually not needed for Render

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1  # Usually not needed for Render
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp  # Change to smtp for production
MAIL_HOST=smtp.mailgun.org  # Example SMTP host
MAIL_PORT=587  # Example SMTP port
MAIL_USERNAME=your-mailgun-username  # Update with your SMTP username
MAIL_PASSWORD=your-mailgun-password  # Update with your SMTP password
MAIL_ENCRYPTION=tls  # Use 'tls' or 'ssl' based on your SMTP provider
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
